#git export 
git clone https://github.com/singh0833/conf.git
rm -rf .git 

#add directory, commit, and push to remote
git add <dir>
git commit -m 'blah'

#assumes you are pushing the 'master' branch 
git push

#svn 'update' to update a repo 
git pull

#remove directory
git rm -r <dir>


